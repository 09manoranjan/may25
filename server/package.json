{
  "name": "server",
  "version": "1.0.0",
  "description": "CRUD Operation for Angular Project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MS",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
